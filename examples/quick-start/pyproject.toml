[project]
name = "quick-start"
version = "0.1.0"
description = "Quick-start example with Socket.IO server in Python and client in Rust"
requires-python = ">=3.14"
dependencies = [
  "python-socketio",
  "uvicorn",
]
