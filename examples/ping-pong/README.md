# Ping Pong Example

This example shows Socket.IO ping-pong between a Python server and a Rust client using integers.

## Prerequisites

- uv with Python >= 3.14

## Running

```shell
cargo run -p ping-pong
```
